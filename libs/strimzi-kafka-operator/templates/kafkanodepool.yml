{{ include "strimzi-kafka-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.kafkanodepools | default dict)
  "resourceKey" "kafkanodepools"
  "kind" "KafkaNodePool"
  "defaultApiVersion" (include "strimzi-kafka-operator.resolveApiVersion" (dict "root" $ "key" "kafkaNodePool" "legacyKey" "strimziKafkaNodePool" "default" "kafka.strimzi.io/v1"))
  "namespaced" true
) }}
