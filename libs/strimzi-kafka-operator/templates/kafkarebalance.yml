{{ include "strimzi-kafka-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.kafkarebalances | default dict)
  "resourceKey" "kafkarebalances"
  "kind" "KafkaRebalance"
  "defaultApiVersion" (include "strimzi-kafka-operator.resolveApiVersion" (dict "root" $ "key" "kafkaRebalance" "legacyKey" "strimziKafkaRebalance" "default" "kafka.strimzi.io/v1"))
  "namespaced" true
) }}
