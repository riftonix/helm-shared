{{ include "strimzi-kafka-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.kafkaconnectors | default dict)
  "resourceKey" "kafkaconnectors"
  "kind" "KafkaConnector"
  "defaultApiVersion" (include "strimzi-kafka-operator.resolveApiVersion" (dict "root" $ "key" "kafkaConnector" "legacyKey" "strimziKafkaConnector" "default" "kafka.strimzi.io/v1"))
  "namespaced" true
) }}
