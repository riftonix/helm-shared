{{ include "strimzi-kafka-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.kafkabridges | default dict)
  "resourceKey" "kafkabridges"
  "kind" "KafkaBridge"
  "defaultApiVersion" (include "strimzi-kafka-operator.resolveApiVersion" (dict "root" $ "key" "kafkaBridge" "legacyKey" "strimziKafkaBridge" "default" "kafka.strimzi.io/v1"))
  "namespaced" true
) }}
