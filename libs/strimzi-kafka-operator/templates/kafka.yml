{{ include "strimzi-kafka-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.kafkas | default dict)
  "resourceKey" "kafkas"
  "kind" "Kafka"
  "defaultApiVersion" (include "strimzi-kafka-operator.resolveApiVersion" (dict "root" $ "key" "kafka" "legacyKey" "strimziKafka" "default" "kafka.strimzi.io/v1"))
  "namespaced" true
) }}
