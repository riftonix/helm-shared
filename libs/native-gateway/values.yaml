# -- Enable native-gateway chart rendering.
enabled: true

# -- Compatibility values inherited from umbrella charts. Accepted but ignored by this chart.
global: {}

# -- Override the default chart label name if needed.
nameOverride: ""

# -- Extra labels applied to every rendered resource.
commonLabels: {}
# -- Extra annotations applied to every rendered resource.
commonAnnotations: {}

# The defaults below are aligned with the current Gateway API standard channel.
# Override them if your cluster uses another bundle version.
apiVersions:
  # -- Default apiVersion for BackendTLSPolicy resources.
  backendTLSPolicy: gateway.networking.k8s.io/v1
  # -- Default apiVersion for GatewayClass resources.
  gatewayClass: gateway.networking.k8s.io/v1
  # -- Default apiVersion for Gateway resources.
  gateway: gateway.networking.k8s.io/v1
  # -- Default apiVersion for GRPCRoute resources.
  grpcRoute: gateway.networking.k8s.io/v1
  # -- Default apiVersion for HTTPRoute resources.
  httpRoute: gateway.networking.k8s.io/v1
  # -- Default apiVersion for ListenerSet resources.
  listenerSet: gateway.networking.k8s.io/v1
  # -- Default apiVersion for ReferenceGrant resources.
  referenceGrant: gateway.networking.k8s.io/v1
  # -- Default apiVersion for TLSRoute resources.
  tlsRoute: gateway.networking.k8s.io/v1

# Each map entry supports:
# - the top-level map key becomes metadata.name
# - namespace: optional for namespaced resources, defaults to the release namespace
# - labels: optional
# - annotations: optional
# - apiVersion: optional override for a single resource
# - spec: arbitrary resource spec
# - set an entry to null in a higher-precedence values file to suppress a lower-precedence default resource
# - status: optional raw status block for fixtures and synthetic manifests

# -- BackendTLSPolicy resources keyed by resource name.
# @default -- {}
backendTLSPolicies:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- GatewayClass resources keyed by resource name.
# @default -- {}
gatewayClasses:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- ""
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- Gateway resources keyed by resource name.
# @default -- {}
gateways:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- GRPCRoute resources keyed by resource name.
# @default -- {}
grpcRoutes:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- HTTPRoute resources keyed by resource name.
# @default -- {}
httpRoutes:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- ListenerSet resources keyed by resource name.
# @default -- {}
listenerSets:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- ReferenceGrant resources keyed by resource name.
# @default -- {}
referenceGrants:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}

# -- TLSRoute resources keyed by resource name.
# @default -- {}
tlsRoutes:
  __helm_docs_example__:
    # -- Namespace for namespaced resources. Defaults to the Helm release namespace. Ignored for cluster-scoped kinds.
    # @default -- release namespace
    namespace: documentation-placeholder
    # -- Resource-specific labels.
    labels: {}
    # -- Resource-specific annotations.
    annotations: {}
    # -- Per-resource apiVersion override.
    # @default -- chart default for this kind
    apiVersion: gateway.networking.k8s.io/v1
    # -- Resource spec. Helm template expressions inside string values are evaluated, so `{{ .Release.Name }}` and `{{ include "..." }}` can be used.
    spec: {}
    # -- Optional resource status. Helm template expressions inside string values are evaluated.
    status: {}
