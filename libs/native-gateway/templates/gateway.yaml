{{ include "native-gateway.renderResources" (dict
  "root" $
  "collection" ($.Values.gateways | default dict)
  "kind" "Gateway"
  "defaultApiVersion" $.Values.apiVersions.gateway
  "namespaced" true
) }}
