{{ include "native-gateway.renderResources" (dict
  "root" $
  "collection" ($.Values.gatewayClasses | default dict)
  "kind" "GatewayClass"
  "defaultApiVersion" $.Values.apiVersions.gatewayClass
  "namespaced" false
) }}
