{{ include "native-gateway.renderResources" (dict
  "root" $
  "collection" ($.Values.tlsRoutes | default dict)
  "kind" "TLSRoute"
  "defaultApiVersion" $.Values.apiVersions.tlsRoute
  "namespaced" true
) }}
