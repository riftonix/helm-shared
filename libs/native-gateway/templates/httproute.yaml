{{ include "native-gateway.renderResources" (dict
  "root" $
  "collection" ($.Values.httpRoutes | default dict)
  "kind" "HTTPRoute"
  "defaultApiVersion" $.Values.apiVersions.httpRoute
  "namespaced" true
) }}
