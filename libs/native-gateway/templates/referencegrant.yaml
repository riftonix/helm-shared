{{ include "native-gateway.renderResources" (dict
  "root" $
  "collection" ($.Values.referenceGrants | default dict)
  "kind" "ReferenceGrant"
  "defaultApiVersion" $.Values.apiVersions.referenceGrant
  "namespaced" true
) }}
