{{ include "vault-secret-operator.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.secretTransformations | default dict)
  "kind" "SecretTransformation"
  "defaultApiVersion" $.Values.apiVersions.secretTransformation
) }}
