{{ include "vault-secret-operator.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.vaultDynamicSecrets | default dict)
  "kind" "VaultDynamicSecret"
  "defaultApiVersion" $.Values.apiVersions.vaultDynamicSecret
) }}
