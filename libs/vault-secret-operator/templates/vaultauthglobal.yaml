{{ include "vault-secret-operator.renderGenericResources" (dict
  "root" $
  "collection" ($.Values.vaultAuthGlobals | default dict)
  "kind" "VaultAuthGlobal"
  "defaultApiVersion" $.Values.apiVersions.vaultAuthGlobal
) }}
