{{- range $name := keys ($.Values.vaultStaticSecrets | default dict) | sortAlpha }}
{{- $secret := get $.Values.vaultStaticSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") $secret }}
{{ include "vault-secret-operator.renderVaultStaticSecret" (dict "root" $ "name" $name "item" $secret) }}
{{ end }}
{{ end }}
