{{ include "keda.renderResources" (dict
  "root" $
  "collection" ($.Values.triggerAuthentications | default dict)
  "kind" "TriggerAuthentication"
  "defaultApiVersion" $.Values.apiVersions.triggerAuthentication
  "namespaced" true
) }}
