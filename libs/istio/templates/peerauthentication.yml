{{ include "istio.renderResources" (dict
  "root" $
  "collection" ($.Values.peerauthentications | default dict)
  "resourceKey" "peerauthentications"
  "kind" "PeerAuthentication"
  "defaultApiVersion" (include "istio.resolveApiVersion" (dict "root" $ "key" "peerAuthentication" "legacyKey" "istioPeerAuthentication" "default" "security.istio.io/v1"))
  "namespaced" true
) }}
