{{ include "istio.renderResources" (dict
  "root" $
  "collection" ($.Values.gateways | default dict)
  "resourceKey" "gateways"
  "kind" "Gateway"
  "defaultApiVersion" (include "istio.resolveApiVersion" (dict "root" $ "key" "gateway" "legacyKey" "istioGateway" "default" "networking.istio.io/v1"))
  "namespaced" true
) }}
