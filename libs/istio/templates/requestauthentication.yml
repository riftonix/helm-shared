{{ include "istio.renderResources" (dict
  "root" $
  "collection" ($.Values.requestauthentications | default dict)
  "resourceKey" "requestauthentications"
  "kind" "RequestAuthentication"
  "defaultApiVersion" (include "istio.resolveApiVersion" (dict "root" $ "key" "requestAuthentication" "legacyKey" "istioRequestAuthentication" "default" "security.istio.io/v1"))
  "namespaced" true
) }}
