{{ include "istio.renderResources" (dict
  "root" $
  "collection" ($.Values.destinationrules | default dict)
  "resourceKey" "destinationrules"
  "kind" "DestinationRule"
  "defaultApiVersion" (include "istio.resolveApiVersion" (dict "root" $ "key" "destinationRule" "legacyKey" "istioDestinationRule" "default" "networking.istio.io/v1"))
  "namespaced" true
) }}
