{{ include "istio.renderResources" (dict
  "root" $
  "collection" ($.Values.virtualservices | default dict)
  "resourceKey" "virtualservices"
  "kind" "VirtualService"
  "defaultApiVersion" (include "istio.resolveApiVersion" (dict "root" $ "key" "virtualService" "legacyKey" "istioVirtualService" "default" "networking.istio.io/v1"))
  "namespaced" true
) }}
