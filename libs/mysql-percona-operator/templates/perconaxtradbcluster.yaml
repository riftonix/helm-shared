{{ include "mysql-percona-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.perconaXtraDBClusters | default dict)
  "kind" "PerconaXtraDBCluster"
  "defaultApiVersion" $.Values.apiVersions.perconaXtraDBCluster
) }}
