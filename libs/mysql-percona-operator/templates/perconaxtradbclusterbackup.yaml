{{ include "mysql-percona-operator.renderResources" (dict
  "root" $
  "collection" ($.Values.perconaXtraDBClusterBackups | default dict)
  "kind" "PerconaXtraDBClusterBackup"
  "defaultApiVersion" $.Values.apiVersions.perconaXtraDBClusterBackup
) }}
