{{- $items := .Values.vmAlerts | default dict }}
{{- range $entryKey := keys $items | sortAlpha }}
{{- $item := index $items $entryKey }}
{{- if kindIs "map" $item }}
---
{{ include "victoria-metrics.renderResource" (dict
  "root" $
  "item" $item
  "kind" "VMAlert"
  "defaultApiVersion" $.Values.apiVersions.vmAlert
  "resourceKey" (printf "vmAlerts[%q]" $entryKey)
  "namespaced" true
) }}
{{- end }}
{{- end }}
