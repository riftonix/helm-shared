{{- $items := .Values.vmScrapeConfigs | default dict }}
{{- range $entryKey := keys $items | sortAlpha }}
{{- $item := index $items $entryKey }}
{{- if kindIs "map" $item }}
---
{{ include "victoria-metrics.renderResource" (dict
  "root" $
  "item" $item
  "kind" "VMScrapeConfig"
  "defaultApiVersion" $.Values.apiVersions.vmScrapeConfig
  "resourceKey" (printf "vmScrapeConfigs[%q]" $entryKey)
  "namespaced" true
) }}
{{- end }}
{{- end }}
