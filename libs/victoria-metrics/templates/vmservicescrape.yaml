{{- $items := .Values.vmServiceScrapes | default dict }}
{{- range $entryKey := keys $items | sortAlpha }}
{{- $item := index $items $entryKey }}
{{- if kindIs "map" $item }}
---
{{ include "victoria-metrics.renderResource" (dict
  "root" $
  "item" $item
  "kind" "VMServiceScrape"
  "defaultApiVersion" $.Values.apiVersions.vmServiceScrape
  "resourceKey" (printf "vmServiceScrapes[%q]" $entryKey)
  "namespaced" true
) }}
{{- end }}
{{- end }}
