{{ include "rabbitmq.renderResources" (dict
  "root" $
  "collection" ($.Values.rabbitmqclusters | default dict)
  "resourceKey" "rabbitmqclusters"
  "kind" "RabbitmqCluster"
  "defaultApiVersion" (include "rabbitmq.resolveApiVersion" (dict "root" $ "key" "rabbitmqCluster" "legacyKey" "rabbitmqCluster" "default" "rabbitmq.com/v1beta1"))
  "namespaced" true
) }}
