{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.imagePolicies | default dict)
  "kind" "ImagePolicy"
  "defaultApiVersion" $.Values.apiVersions.imagePolicy
  "namespaced" true
) }}
