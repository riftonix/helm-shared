{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.ociRepositories | default dict)
  "kind" "OCIRepository"
  "defaultApiVersion" $.Values.apiVersions.ociRepository
  "namespaced" true
) }}
