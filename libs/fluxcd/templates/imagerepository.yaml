{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.imageRepositories | default dict)
  "kind" "ImageRepository"
  "defaultApiVersion" $.Values.apiVersions.imageRepository
  "namespaced" true
) }}
