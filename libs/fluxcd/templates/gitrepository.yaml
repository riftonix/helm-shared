{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.gitRepositories | default dict)
  "kind" "GitRepository"
  "defaultApiVersion" $.Values.apiVersions.gitRepository
  "namespaced" true
) }}
