{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.receivers | default dict)
  "kind" "Receiver"
  "defaultApiVersion" $.Values.apiVersions.receiver
  "namespaced" true
) }}
