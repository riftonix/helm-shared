{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.providers | default dict)
  "kind" "Provider"
  "defaultApiVersion" $.Values.apiVersions.provider
  "namespaced" true
) }}
