{{ include "fluxcd.renderResources" (dict
  "root" $
  "collection" ($.Values.artifactGenerators | default dict)
  "kind" "ArtifactGenerator"
  "defaultApiVersion" $.Values.apiVersions.artifactGenerator
  "namespaced" true
) }}
