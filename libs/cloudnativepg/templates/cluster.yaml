{{ include "cloudnativepg.renderResources" (dict
  "root" $
  "collection" ($.Values.clusters | default dict)
  "kind" "Cluster"
  "defaultApiVersion" $.Values.apiVersions.cluster
  "namespaced" true
) }}
