{{ include "cloudnativepg.renderResources" (dict
  "root" $
  "collection" ($.Values.imageCatalogs | default dict)
  "kind" "ImageCatalog"
  "defaultApiVersion" $.Values.apiVersions.imageCatalog
  "namespaced" true
) }}
