{{ include "cloudnativepg.renderResources" (dict
  "root" $
  "collection" ($.Values.publications | default dict)
  "kind" "Publication"
  "defaultApiVersion" $.Values.apiVersions.publication
  "namespaced" true
) }}
