{{ include "cloudnativepg.renderResources" (dict
  "root" $
  "collection" ($.Values.poolers | default dict)
  "kind" "Pooler"
  "defaultApiVersion" $.Values.apiVersions.pooler
  "namespaced" true
) }}
