{{- range $name := keys ($.Values.fakes | default dict) | sortAlpha }}
{{- $item := get $.Values.fakes $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "Fake" "apiVersionKey" "fake") }}
{{- end }}
{{- end }}
