{{- range $name := keys ($.Values.clusterSecretStores | default dict) | sortAlpha }}
{{- $item := get $.Values.clusterSecretStores $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderClusterResource" (dict "root" $ "name" $name "item" $item "kind" "ClusterSecretStore" "apiVersionKey" "clusterSecretStore") }}
{{- end }}
{{- end }}
