{{- range $name := keys ($.Values.uuids | default dict) | sortAlpha }}
{{- $item := get $.Values.uuids $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "UUID" "apiVersionKey" "uuid") }}
{{- end }}
{{- end }}
