{{- range $name := keys ($.Values.cloudsmithAccessTokens | default dict) | sortAlpha }}
{{- $item := get $.Values.cloudsmithAccessTokens $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "CloudsmithAccessToken" "apiVersionKey" "cloudsmithAccessToken") }}
{{- end }}
{{- end }}
