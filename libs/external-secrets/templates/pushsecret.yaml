{{- range $name := keys ($.Values.pushSecrets | default dict) | sortAlpha }}
{{- $item := get $.Values.pushSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "PushSecret" "apiVersionKey" "pushSecret") }}
{{- end }}
{{- end }}
