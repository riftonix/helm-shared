{{- range $name := keys ($.Values.secretStores | default dict) | sortAlpha }}
{{- $item := get $.Values.secretStores $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "SecretStore" "apiVersionKey" "secretStore") }}
{{- end }}
{{- end }}
