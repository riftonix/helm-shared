{{- range $name := keys ($.Values.stsSessionTokens | default dict) | sortAlpha }}
{{- $item := get $.Values.stsSessionTokens $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "STSSessionToken" "apiVersionKey" "stsSessionToken") }}
{{- end }}
{{- end }}
