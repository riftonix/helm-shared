{{- range $name := keys ($.Values.vaultDynamicSecrets | default dict) | sortAlpha }}
{{- $item := get $.Values.vaultDynamicSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "VaultDynamicSecret" "apiVersionKey" "vaultDynamicSecret") }}
{{- end }}
{{- end }}
