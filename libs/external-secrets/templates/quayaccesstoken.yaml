{{- range $name := keys ($.Values.quayAccessTokens | default dict) | sortAlpha }}
{{- $item := get $.Values.quayAccessTokens $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "QuayAccessToken" "apiVersionKey" "quayAccessToken") }}
{{- end }}
{{- end }}
