{{- range $name := keys ($.Values.ecrAuthorizationTokens | default dict) | sortAlpha }}
{{- $item := get $.Values.ecrAuthorizationTokens $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "ECRAuthorizationToken" "apiVersionKey" "ecrAuthorizationToken") }}
{{- end }}
{{- end }}
