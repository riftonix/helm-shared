{{- range $name := keys ($.Values.clusterExternalSecrets | default dict) | sortAlpha }}
{{- $item := get $.Values.clusterExternalSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderClusterResource" (dict "root" $ "name" $name "item" $item "kind" "ClusterExternalSecret" "apiVersionKey" "clusterExternalSecret") }}
{{- end }}
{{- end }}
