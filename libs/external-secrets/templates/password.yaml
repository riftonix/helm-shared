{{- range $name := keys ($.Values.passwords | default dict) | sortAlpha }}
{{- $item := get $.Values.passwords $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "Password" "apiVersionKey" "password") }}
{{- end }}
{{- end }}
