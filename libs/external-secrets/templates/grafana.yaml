{{- range $name := keys ($.Values.grafanas | default dict) | sortAlpha }}
{{- $item := get $.Values.grafanas $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "Grafana" "apiVersionKey" "grafana") }}
{{- end }}
{{- end }}
