{{- range $name := keys ($.Values.externalSecrets | default dict) | sortAlpha }}
{{- $item := get $.Values.externalSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "ExternalSecret" "apiVersionKey" "externalSecret") }}
{{- end }}
{{- end }}
