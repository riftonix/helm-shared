{{- range $name := keys ($.Values.webhooks | default dict) | sortAlpha }}
{{- $item := get $.Values.webhooks $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderNamespacedResource" (dict "root" $ "name" $name "item" $item "kind" "Webhook" "apiVersionKey" "webhook") }}
{{- end }}
{{- end }}
