{{- range $name := keys ($.Values.clusterPushSecrets | default dict) | sortAlpha }}
{{- $item := get $.Values.clusterPushSecrets $name -}}
{{- if and (ne $name "__helm_docs_example__") (include "external-secrets.isActiveItem" $item) }}
---
{{ include "external-secrets.renderClusterResource" (dict "root" $ "name" $name "item" $item "kind" "ClusterPushSecret" "apiVersionKey" "clusterPushSecret") }}
{{- end }}
{{- end }}
