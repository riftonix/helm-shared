{{- range $key, $item := .Values.middlewareTCPs }}
{{- if kindIs "map" $item }}
{{- if (or (not (hasKey $item "enabled")) $item.enabled) }}
---
{{ include "traefik.renderResource" (dict
  "root" $
  "item" $item
  "kind" "MiddlewareTCP"
  "defaultApiVersion" $.Values.apiVersions.middlewareTCP
  "resourceKey" (printf "middlewareTCPs.%v" $key)
  "namespaced" true
) }}
{{- end }}
{{- end }}
{{- end }}
