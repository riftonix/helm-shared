{{- range $key, $item := .Values.serversTransports }}
{{- if kindIs "map" $item }}
{{- if (or (not (hasKey $item "enabled")) $item.enabled) }}
---
{{ include "traefik.renderResource" (dict
  "root" $
  "item" $item
  "kind" "ServersTransport"
  "defaultApiVersion" $.Values.apiVersions.serversTransport
  "resourceKey" (printf "serversTransports.%v" $key)
  "namespaced" true
) }}
{{- end }}
{{- end }}
{{- end }}
