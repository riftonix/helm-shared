{{- range $key, $item := .Values.ingressRouteTCPs }}
{{- if kindIs "map" $item }}
{{- if (or (not (hasKey $item "enabled")) $item.enabled) }}
---
{{ include "traefik.renderResource" (dict
  "root" $
  "item" $item
  "kind" "IngressRouteTCP"
  "defaultApiVersion" $.Values.apiVersions.ingressRouteTCP
  "resourceKey" (printf "ingressRouteTCPs.%v" $key)
  "namespaced" true
) }}
{{- end }}
{{- end }}
{{- end }}
