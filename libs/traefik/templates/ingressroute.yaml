{{- range $key, $item := .Values.ingressRoutes }}
{{- if kindIs "map" $item }}
{{- if (or (not (hasKey $item "enabled")) $item.enabled) }}
---
{{ include "traefik.renderResource" (dict
  "root" $
  "item" $item
  "kind" "IngressRoute"
  "defaultApiVersion" $.Values.apiVersions.ingressRoute
  "resourceKey" (printf "ingressRoutes.%v" $key)
  "namespaced" true
) }}
{{- end }}
{{- end }}
{{- end }}
