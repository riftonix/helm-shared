include:
  - project: $CI_INCLUDE_PATH
    ref: $CI_INCLUDE_REF
    file:
      - "/variables-subchart.gitlab-ci.yml"

  - project: $CI_INCLUDE_PATH
    ref: $CI_INCLUDE_REF
    file:
      - "/publish-subchart.gitlab-ci.yml"

stages:
  - validate-tag
  - package
  - publish
