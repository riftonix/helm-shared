{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "argocd values",
  "description": "Schema for argocd Helm chart values.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable rendering for this subchart."
    },
    "global": {
      "type": "object",
      "default": {},
      "description": "Compatibility values inherited from umbrella charts. Accepted but ignored by this chart.",
      "additionalProperties": true
    },
    "nameOverride": {
      "type": "string",
      "description": "Override the chart name used in generated labels."
    },
    "commonLabels": {
      "$ref": "#/definitions/stringMap",
      "description": "Labels merged into every rendered resource."
    },
    "commonAnnotations": {
      "$ref": "#/definitions/stringMap",
      "description": "Annotations merged into every rendered resource."
    },
    "apiVersions": {
      "type": "object",
      "description": "Default API version overrides per supported Argo CD kind.",
      "additionalProperties": false,
      "properties": {
        "application": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "applicationSet": {
          "$ref": "#/definitions/nonEmptyString"
        },
        "appProject": {
          "$ref": "#/definitions/nonEmptyString"
        }
      }
    },
    "applications": {
      "$ref": "#/definitions/resourceMap",
      "description": "Map of Application resources keyed by resource name."
    },
    "applicationSets": {
      "$ref": "#/definitions/resourceMap",
      "description": "Map of ApplicationSet resources keyed by resource name."
    },
    "appProjects": {
      "$ref": "#/definitions/resourceMap",
      "description": "Map of AppProject resources keyed by resource name."
    }
  },
  "definitions": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "stringMap": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "type": "string",
        "minLength": 1
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "freeFormObject": {
      "type": "object",
      "description": "Arbitrary Argo CD payload passed through by the chart.",
      "additionalProperties": true
    },
    "resourceItem": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "namespace": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Namespace for namespaced resources. Ignored for cluster-scoped kinds."
        },
        "labels": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific labels."
        },
        "annotations": {
          "$ref": "#/definitions/stringMap",
          "description": "Resource-specific annotations."
        },
        "apiVersion": {
          "$ref": "#/definitions/nonEmptyString",
          "description": "Per-resource API version override."
        },
        "spec": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Resource spec rendered as-is."
        },
        "status": {
          "$ref": "#/definitions/freeFormObject",
          "description": "Optional resource status rendered as-is."
        }
      }
    },
    "resourceMap": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "$ref": "#/definitions/nonEmptyString"
      },
      "additionalProperties": {
        "anyOf": [
          {
            "$ref": "#/definitions/resourceItem"
          },
          {
            "type": "null"
          }
        ]
      }
    }
  }
}
