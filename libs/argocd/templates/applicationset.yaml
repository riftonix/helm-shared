{{ include "argocd.renderResources" (dict
  "root" $
  "collection" ($.Values.applicationSets | default dict)
  "kind" "ApplicationSet"
  "defaultApiVersion" $.Values.apiVersions.applicationSet
  "namespaced" true
) }}
